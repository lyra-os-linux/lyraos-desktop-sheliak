{
  "name": "sheliak",
  "version": "1.12.3",
  "private": true,
  "description": "Dock nativo do Lyra OS para GNOME Shell 48",
  "license": "GPL-3.0-or-later",
  "type": "module",
  "scripts": {
    "clean": "rm -rf dist",
    "build": "node scripts/build.mjs",
    "check": "tsc --noEmit",
    "test": "npm run build && node tests/test-i18n.mjs",
    "pack": "npm run build && cd dist && zip -qr ../sheliak@lyraos.com.br.zip ."
  },
  "devDependencies": {
    "@girs/gnome-shell": "^48.0.0",
    "esbuild": "^0.25.0",
    "typescript": "^5.8.0"
  }
}
